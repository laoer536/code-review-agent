# 安全规范

## 输入验证
- 所有外部输入必须校验（用户输入、API 参数、文件内容）
- 使用 zod 或 joi 做参数校验
- SQL 查询必须用参数化查询，禁止字符串拼接

## 敏感信息
- 禁止硬编码密钥、密码、token
- 使用环境变量或密钥管理服务
- 日志中不能输出敏感信息

## 常见漏洞
- 防止 XSS：用户内容必须转义后再渲染
- 防止 CSRF：关键操作需要验证 token
- 防止路径遍历：文件操作必须校验路径
- 使用 crypto.randomUUID() 生成 ID，不要用 Math.random()
